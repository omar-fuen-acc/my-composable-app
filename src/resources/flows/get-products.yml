flow:
  id: 'get-products'
  description: 'Returns the full list of products from the mock data file'
  ttl: 10s

first.task: 'v1.get.products'

tasks:
  - input: []
    process: 'v1.get.products'
    output:
      - 'result -> output.body'
    description: 'Read products from mock file and return as response body'
    execution: end
